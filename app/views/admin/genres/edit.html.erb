<div class="container">
  <div class="row">
    <div class="admin-genre-edit">
      <div class="title">
        <p>ジャンル編集</p>
      </div>
      <div class="admin-genre-edit-form">
        <%= form_with model: @genre, url:admin_genre_path, local:true do |f| %>
　  　    ジャンル名&emsp;
　      　<%= f.text_field :name %>&emsp;
　        <%= f.submit '変更を保存', class:"btn btn-sm btn-success" %>
　      <% end %>
      </div>
    </div>
  </div>
</div>
