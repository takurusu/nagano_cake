<div class="container">
  <div class="row">
    <div class="cart-item-index">
    <div class="title">
      <p>ショッピングカート</p>
    </div>
    <div class="cart-item-index-contents">
      <table class="table table-stripped">
        <tr>
          <th>商品名</th>
          <th>単価（税込)</th>
          <th>数量</th>
          <th>小計</th>
          <th></th>
        </tr>
          <% @cart_items.each do |cart_item| %>
          <tr>
            <td>
              <%= attachment_image_tag cart_item.item, :image_id, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg" %>
              <%= cart_item.item.name %>
            </td>
            <td><%= (cart_item.item.price*1.08).to_i %></td>
            <td>
              <%= form_with model: cart_item, url: cart_item_path(cart_item), local: true, method: :patch do |f| %>
                <%= f.number_field :amount, min: 1, max:10, id: "amount_cart"%>
                <%= f.submit "変更",class: "btn btn-sm btn-success" %>
              <% end %>
            </td>
            <td><% subtotal = ((cart_item.item.price*cart_item.amount)*1.08).to_i %><%= subtotal %></td>
            <td><%= link_to "削除する", cart_item_path(cart_item), method: :delete, class:"btn btn-sm btn-danger" %></td>
          </tr>
          <% end %>
          <tr>
            <th><%= link_to "買い物を続ける", items_path, class:"btn btn-sm btn-primary" %></th>
            <th>合計金額</th>
            <td></td>
          </tr>
      </table>
      <div class="link">
        <%= link_to "情報入力に進む", new_order_path, class:"btn btn-sm btn-success" %>
      </div>
    </div>
  </div>
</div>