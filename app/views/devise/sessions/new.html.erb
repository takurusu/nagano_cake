<div class="container">
  <div class="row">
    <div class="customer-sign-in">
      <div class="col-md-12">
        <div class="title">
          <p>ログイン</p>
        </div>
        <div class="title">
          <p>会員の方はこちらからログイン</p>
        </div>
        <table>
        <%= form_with model: @customer, url: new_customer_session_path, local: true do |f| %>
        <tr>
          <th><%= f.label :メールアドレス %></th>
          <td>&emsp;<%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
        </tr>
        <tr>
          <th><%= f.label :パスワード %></th>
          <td>&emsp;<%= f.password_field :password, autocomplete: "current-password" %></td>
        </tr>
        </table>
        <div class="actions">
          <%= f.submit "ログイン", class:"btn btn-sm btn-primary" %>
        </div>
        <% end %>
      <div class=customer-sign-up>
        <div class="title">
          <p>会員登録がお済みでない方</p>
        </div>
        <p><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください。</p>
      </div>
      </div>
    </div>
  </div>
</div>
